# C?u hình c?ng server (có th? ???c ghi ?è b?i bi?n môi tr??ng PORT trên các n?n t?ng deploy)
server.port=${PORT:8080}
# C?u hình Database t? bi?n môi tr??ng (S?n sàng cho deploy)
# Các n?n t?ng nh? Heroku, Render s? t? ??ng cung c?p bi?n DATABASE_URL.
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# C?u hình JPA/Hibernate
# 'update' phù h?p cho phát tri?n, nh?ng 'validate' ho?c không có gì ('none') an toàn h?n cho production.
# Gi? 'update' ?? ti?n cho l?n deploy ??u tiên, nó s? t? t?o b?ng.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false # T?t log SQL trong production ?? t?ng hi?u su?t
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect